import { SortIcon } from "@/components/shared/Icons/Icons";
import { Button } from "@/components/ui/button";

const Sort = (): JSX.Element => {
  return (
    <>
      <Button variant="outline" >
        <SortIcon />
      </Button>
    </>
  );
};

export default Sort;
